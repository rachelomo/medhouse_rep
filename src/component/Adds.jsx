import { Link } from "react-router-dom";

const Adds = () => {
  return (
    <div className="adds">
      <Link to="/adding" style={{ textDecoration: "none", color: " #f8c8dc" }}>
        Tenant want Ads
      </Link>
    </div>
  );
};

export default Adds;
